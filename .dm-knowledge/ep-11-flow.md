- **[convention]** v1.1 protocol gating: epicenter.client.record_capabilities() stores what the handshake announced and client.supports(method, {bufnr}) answers it - a navgraph/* method is trusted only when listed in experimental.navgraph.methods, a standard-LSP one only when its server capability flag is set (client.STANDARD_CAPABILITY maps them). A v1.0 server is therefore never sent a 1.1 method; features show a 'needs protocol 1.1' notice instead. client.register_session(root, session, announced) takes a stand-in handshake so a spec can drive the gated paths.  _(2026-08-28T09:40:03Z)_
- **[convention]** The fake server now speaks the v1.1 addendum: tests/fake/v11.lua owns navgraph/tests, navgraph/types, navgraph/impact and the standard call/type-hierarchy methods; tests/fake/core.lua's symbolAt gained range+breadcrumbs and status gained protocolMinor/backend. tests/fakelib/server.lua checks params AND results for ANY method tests/contract/schema.lua defines (not just navgraph/*), so a standard-LSP addition is contract-checked too.  _(2026-08-28T09:40:03Z)_
- **[pitfall]** The generated vimdoc tables are capped at 78 columns and the column widths are the MAX over every row: adding a long subcommand name (or a long *epicenter-<feature>* tag) pushes every OTHER row over the cap. Budget: 4 + (longest command + 2) + (longest tag + 2) + desc <= 78. Keep a new command's desc under ~40 chars, or make docs-check fails on rows you did not touch.  _(2026-08-28T09:40:04Z)_
- **[pitfall]** A Lua list literal with a nil in the middle ends ipairs right there. Bit twice this wave: ui/peek.lua's borrowed-key map (a key with no prior mapping vanished, so it was never handed back) and crumbs' fan_of candidate list. Build such lists by insertion, or chain explicit `a or b or c`.  _(2026-08-28T09:40:04Z)_
- **[pitfall]** client.register_session(root, ...) OVERWRITES whatever record that root already has, including a live fake server's client_id - client.stop() then deletes the record without stopping the process, and the NEXT spec file's support.start_fake() times out (30s per test, whole suite 10x slower). A spec that registers a stand-in session must use its own temp root (mkdir a .navgraph marker and edit a file inside it), never the shared fixture root the current buffer resolves to.  _(2026-08-28T09:52:03Z)_
- **[pitfall]** A spec that leaves an ordinary split open leaks it into the NEXT spec file: harness.isolate() closes floating windows only, and headless Neovim then aborts in grid_line_flush (grid_line_clear_to <= grid_line_maxcol). Close any split a spec opens in its own after_each.  _(2026-08-28T10:12:24Z)_
- **[pitfall]** Neovim 0.11.0 only: tests/cases/client_integration_spec.lua 'restarts with the caller's cmd instead of silently keeping the running one' fails reproducibly, in isolation too, and fails with the 1.1 work stashed - it predates this wave. 0.10.4 and 0.12.4 are green. Diagnose against 0.11's vim.lsp.start/client:stop timing before the release gate.  _(2026-08-28T10:43:41Z)_
- **[convention]** Per-project state that must survive a restart goes through lua/epicenter/store.lua: one JSON file per (kind, root) under stdpath('state'), never in the project. store.set_root(dir) is the test seam - every spec touching impact approvals or palette frecency must point it at a tempdir.  _(2026-08-28T10:43:41Z)_
- **[pitfall]** epicenter.root.find()/client.lua keyed server records by `vim.fs.normalize(path)` alone, no symlink resolution. macOS's tempdir traverses `/var` -> `/private/var`; Neovim resolves that symlink when it sets a buffer's name but `vim.fn.tempname()` does not, so a root registered from the raw tempname and the root `root.find()` derived from the buffer disagreed as strings, silently missing a live session (6 macOS-only CI failures, PR #3). Fixed by `root.normalize()` (normalize + `uv.fs_realpath`, falling back when the path does not exist yet), used everywhere a root or root-relative path is computed or compared - never call `vim.fs.normalize` alone on a root.  _(2026-08-28T13:05:20Z)_
- **[pitfall]** A spec that opens a real quickfix window (copen) or leaves a toast float up aborts headless Neovim in the NEXT test of the same file (grid_line_flush assertion) - harness.isolate() only cleans between FILES. Either close both in the same test (toast.clear() too, not just cclose), or capture ui.qf.send_and_notify instead of letting it open a window.  _(2026-08-28T14:33:07Z)_
- **[convention]** Impact approvals are scoped to a change: approvals.load(root, change_id) returns a state carrying the change it is read against, approved() requires entries[key] == state.change_id and set(state, symbol, approved) writes under that same one. Reading approvals without a change is what let twelve ticks earned on one edit survive into the next one. save() merges the file it replaces (minus state.revoked) so a second Neovim's approvals are not clobbered.  _(2026-08-28T14:33:07Z)_
- **[pitfall]** A repaint that clears a namespace and re-places extmarks from the SERVER's line numbers snaps them back onto whatever moved into those lines. ui/marklayer.lua owns the fix for impact.marks and blast.ripples: keep bufnr -> line -> extmark id and update in place (opts.id), reading each mark's current row back out first.  _(2026-08-28T14:33:07Z)_
