- **[command]** make test-real runs tests/real/*_spec.lua against the REAL navgraph lsp over tests/fixtures/real (32 files: py/go/lua). NAVGRAPH_BIN picks the binary, default navgraph on PATH; tests/run_real.lua preflights it with one initialize exchange over a pipe, so a binary without an 'lsp' subcommand is one line, not a wall of timeouts.  _(2026-08-28T01:04:01Z)_
- **[pitfall]** Neovim's make_client_capabilities() advertises window.workDoneProgress, and navgraph answers it with a window/workDoneProgress/create carrying a STRING id, then answers the client's reply to that as if the reply were a request. Neovim raises on the resulting response id it never issued. epicenter.client.capabilities() sets window.workDoneProgress=false for that reason; tests/real/progress_spec.lua pins it.  _(2026-08-28T01:04:01Z)_
- **[invariant]** navgraph/hot ranks by fanInExact (connectivity), NOT by fanIn, so items[1] is not the widest bar - features/hot.lua scales with hot.bar_scale() over the whole list.  _(2026-08-28T01:04:01Z)_
- **[convention]** tests/contract/ is the protocol as data: lsp.md is a verbatim copy of NavGraph's docs/lsp.md and schema.lua is one params + one result shape per navgraph/* method. The fake server checks params STRICTLY (unknown/ill-typed param -> -32602) and its own results forward-compatibly; tests/cases/contract_spec.lua drives every feature through a recording session and validates what it sent; tests/real/contract_spec.lua validates the real server's answers. Adding a client helper without a schema fails the suite.  _(2026-08-28T01:23:12Z)_
- **[pitfall]** A stale-drop channel name must be per PANEL, not per session: two dashboards sharing channel='status' meant the second's request superseded the first's answer and left it on 'loading...'. features/core.lua now uses status:<bufnr>.  _(2026-08-28T01:23:12Z)_
- **[convention]** The keymap table, command table and config reference in README.md and doc/epicenter.txt are GENERATED from lua/epicenter/registry.lua + config defaults by lua/epicenter/docs.lua. Regions are delimited by <!-- registry:<name> --> in markdown and by a *epicenter-<name>-table* tag plus a > ... < literal block in vimdoc. make docs-check diffs them (CI runs it), make docs-fix rewrites them. Edit the feature spec's desc/option_docs, never the table. Generated vimdoc lines are capped at 78 columns, README at 100.  _(2026-08-28T01:31:39Z)_
- **[pitfall]** Neovim 0.10 vs 0.11 differences this plugin hits, all funnelled through lua/epicenter/compat.lua: 0.11 made the LSP client method-call (client:request/:notify/:stop/:cancel_request/:supports_method) while 0.10 keeps plain functions - client:stop() on 0.10 passes the client as 'force', which SIGTERMs a server blocked on a read and hangs the suite; vim.validate's flat (name, value, type) form is 0.11+; and vim.lsp.start's 'attach' option is 0.11+, so 0.10 uses vim.lsp.start_client plus an explicit buf_attach_client (plain vim.lsp.start would silently attach the current buffer). Verify with NVIM=<0.10 binary> make test.  _(2026-08-28T01:36:38Z)_
- **[convention]** lsp/navgraph.lua is the Neovim 0.11+ server definition for vim.lsp.enable('navgraph'); it reads epicenter.config and epicenter.client.FILETYPES so it cannot drift from the plugin's own vim.lsp.start path, and tests/cases/lsp_config_spec.lua pins that. Pair it with lsp.auto_start=false or two navgraph clients attach.  _(2026-08-28T01:36:38Z)_
- **[command]** make screenshots regenerates assets/*.svg: scripts/screenshots.sh runs a real nvim inside a 120x36 tmux pane (own -L socket and own $HOME, plus a throwaway copy of tests/fixtures/real, so it cannot touch the user's tmux and committed paths read ~/demo), captures with capture-pane -p -e -N, and scripts/ansi2svg.lua converts the SGR grid. Needs tmux and a navgraph binary (NAVGRAPH_BIN).  _(2026-08-28T02:10:57Z)_
- **[pitfall]** ansi2svg has to satisfy two renderers that disagree. Chromium (so GitHub) honours a per-glyph x list but COLLAPSES runs of spaces in SVG text whatever xml:space says; librsvg keeps the spaces but ignores every x past the first and advances by the font; neither honours textLength. The form both render: one x per glyph, each space written as &#160;, and the run trimmed to its ink so the first x is a real glyph's. Verify a change with BOTH rsvg-convert and chromium --headless --screenshot.  _(2026-08-28T02:10:57Z)_
- **[pitfall]** navgraph --version answers with a JSON capabilities document, not a version string - health.lua takes build.buildVersion out of it, else :checkhealth is 30KB of JSON.  _(2026-08-28T02:10:57Z)_
- **[pitfall]** A float's Window:valid()/Panel:valid() goes false the moment close() starts, but the fade-out still owns a real window AND the focus for another frame. A test that reads nvim_get_current_win() right after a close gets the dying float. Wait on #nvim_list_wins() instead.  _(2026-08-28T02:10:57Z)_
- **[convention]** tests/cases/perf_guard_spec.lua counts at two seams: session.rpc.request (every navgraph/* the plugin sends) and animate.uv_driver (every tween on a real timer). It must clear vim.g.epicenter_reduce_motion, which tests/minimal_init.lua sets, or the tween counter reads zero whatever the plugin does.  _(2026-08-28T02:10:57Z)_
