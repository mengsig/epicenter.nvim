- **[decision]** The blast panel never repaints a realtime update wholesale. features/blast/model.lua keys every node by uri#qualified and defines one total order (ring, file, line, key); model.transition() returns the union of the old and new node sets in that order, each flagged added or removed. The panel paints that union - arriving rows get an EpicenterRange line highlight, departing rows render dimmed and stay in place - then one tween ticks only the chips line (Panel:_paint_chips rewrites buffer line 2) and its on_done swaps in the new set. A node that only changes ring therefore moves instead of being removed and re-added, and the whole diff is testable without a UI because model.diff/model.transition are pure.  _(2026-08-27T20:56:15Z)_
- **[convention]** The blast panel owns its buffer instead of handing it to ui.list: the header must stay pinned above the body and a realtime update must touch only the rows that changed. It still uses the kit's pure layer - ui.list.render, ui.list.scroll, ui.animate - so scrolling and motion match every other widget. HEADER_ROWS = 3 (title, chips, blank); body spans are offset by it.  _(2026-08-27T20:56:15Z)_
- **[convention]** A feature spec can now carry option_rules = { variants = {path -> types}, enums = {path -> values}, positive = {path -> true} } to validate its own options at setup() time (epicenter.registry merges them into epicenter.config; a feature may only rule on paths rooted at an option key it owns), and setup = function(cfg) to install session-wide autocmds or buffer-local keys. epicenter.setup() calls every feature's setup at the end, so it must be idempotent.  _(2026-08-27T20:56:15Z)_
- **[pitfall]** Setting vim.o.columns inside a headless spec aborts Neovim in draw_tabline (tab_page_click_defs_size >= Columns). Leave the editor size alone in tests; the default 80x24 is enough for every panel assertion.  _(2026-08-27T20:56:15Z)_
- **[pitfall]** tests/harness.lua isolate() clears package.loaded between spec files but not autocmds, so an autocmd installed by an earlier file still calls the OLD module instance while the spec holds a NEW one - two copies of the module's state. A spec that asserts on state an autocmd also writes must re-run require('epicenter').setup() in before_each to rewire the autocmds to its own instance.  _(2026-08-27T20:56:15Z)_
- **[pitfall]** tests/run.lua takes a spec-name pattern as its argument, but nvim -l does not deliver it: the run is always the full suite. To run one spec file, load tests/harness.lua from a small driver script and pass that one path to harness.run().  _(2026-08-27T20:56:15Z)_
- **[invariant]** The editor protocol contract is docs/lsp.md v1 in the NavGraph repo, not the draft protocol.md. Field names that bite: a blast node is { symbol, depth, via, exact } (not ring/heuristic - exact is the INVERSE of heuristic); counts come from blast.summary { symbols, files, tests, maxDepth, truncated, byDepth, byFile } and must not be recounted client-side; navgraph/callers answers { root: Node } with Node { symbol, exact, lines, children, ext, recursion } and takes no limit param; navgraph/outline takes { path } (a path SUBSTRING filter, never a uri) and answers { files: [{file, lang, symbols}] }; navgraph/diff answers { ref, blast } wrapping a whole blast result. Symbol.line is 1-based, LSP positions 0-based. tests/fake/blast.lua rejects any param outside the contract with -32602, and tests/cases/blast_protocol_spec.lua asserts the exact key sets.  _(2026-08-27T21:07:57Z)_
- **[pitfall]** A jump out of a float must call nvim_set_current_win on the target window BEFORE :edit. With motion off the float closes inside close(), so the ordering bug is invisible; with animation on the float is still current and the edit lands in it. tests/cases/blast_jump_spec.lua runs both the panel and the hover card with vim.g.epicenter_reduce_motion cleared to keep that covered.  _(2026-08-27T21:07:57Z)_
