- **[convention]** Lazy tree panels: the server returns a degree per edge, so a node knows it has children before fetching them. ui.tree derives expandable from #children>0, so an unfetched node carries one { type = "pending" } placeholder child to keep its chevron; l fires the request and opens the row, and the arriving level replaces the placeholder via Tree:set_expanded(key, open).  _(2026-08-27T20:37:45Z)_
- **[invariant]** Explorer node keys are uri#qualified@line, never a server-assigned id: that is what makes ui.tree's cycle detection and the EpicenterIndexed diff (features/explore.lua merge_children keeps a loaded subtree whose key survives) both stable across a reindex.  _(2026-08-27T20:37:45Z)_
- **[pitfall]** Setting vim.o.columns in a spec that runs early in the alphabetical order aborts nvim 0.12 with a draw_tabline assertion (tab_page_click_defs_size >= Columns) and takes the whole run down; palette_e2e_spec gets away with it only because it runs late. New specs must not resize the editor - the default 80x24 is enough.  _(2026-08-27T20:37:45Z)_
- **[convention]** Two panel widgets: ui.palette is prompt+list+preview for type-to-filter, ui.panel is the float carrying a ui.list or ui.tree plus the shared jump/peek/yank/close keys for a result set that is already computed.  _(2026-08-27T20:37:45Z)_
