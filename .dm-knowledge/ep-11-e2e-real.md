- **[pitfall]** NavGraph 1.1's real navgraph/impact changeId is deliberately keyed on hunk SHAPE (file+range touched), not content bytes (src/lsp/queries.zig changeId()) - re-editing the same line again keeps the same changeId. Per-symbol staleness is Symbol.contentHash's job (approvals.lua): only editing an impacted definition's OWN source changes ITS hash and invalidates its approval, not a fresh edit elsewhere with an identical hunk shape.  _(2026-08-28T17:03:33Z)_
- **[pitfall]** Real NavGraph 1.1 is materially more accurate at python call resolution than whatever pre-1.1 build tests/real/explore_spec.lua and navigate_spec.lua were originally written against: it now correctly types self.<field>.method() through instance-field types (self.items.get -> ItemService.get, not the enclosing class's own get) and correctly excludes a dict receiver's .get() as an external stdlib call rather than a heuristic match to a same-named project symbol. A real-lane test asserting the OLD (heuristic) answer will fail against a real 1.1 server; verify the actual navgraph calls/path output by hand before assuming the plugin is wrong.  _(2026-08-28T17:03:33Z)_
