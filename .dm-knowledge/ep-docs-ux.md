- **[pitfall]** assets/hierarchy.svg, tests.svg, review.svg (v1.1-gated panels) cannot be regenerated in this environment - the real navgraph on $PATH/local install is protocol 1.0 only, and NavGraph's own latest release/build is v1.0.0 (no v1.1 addendum anywhere upstream yet). scripts/screenshots.sh times out on those three against a 1.0 server; the committed assets are already current (generated at the exact 1.1.0-landing commit 03ef5c2, include the v1.1 'users' group) - leave them as-is rather than committing an error-panel capture until a real v1.1-speaking navgraph exists.  _(2026-08-28T15:57:53Z)_
- **[convention]** Protocol-1.1 version-gate messaging is inconsistent across features: :Epicenter review/impact show the 'needs navgraph protocol 1.1' notice as a transient toast (epicenter.notify, easy to miss), while :Epicenter hierarchy/types/tests show the identical situation as static text inside the opened panel body (persists until closed). Same failure mode (client.unsupported_reason), two different UI channels - a candidate UX fix, not done here (docs-only task).  _(2026-08-28T15:57:53Z)_
