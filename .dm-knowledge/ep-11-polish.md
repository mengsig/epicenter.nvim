- **[convention]** client.gate(bufnr, method, what, panel?) is the single entry for the protocol-1.1 gate notice: writes into an already-open panel via panel:notice(text) when given one, else toasts. blast/panel.lua and impact/review.lua thread a gate_reason through open()/set_query() so a gated panel-shaped feature opens showing the notice rather than firing a request the server would refuse.  _(2026-08-28T17:49:25Z)_
