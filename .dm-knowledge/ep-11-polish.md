- **[convention]** client.gate(bufnr, method, what, panel?) is the single entry for the protocol-1.1 gate notice: writes into an already-open panel via panel:notice(text) when given one, else toasts. client.gate_notice(bufnr, method, what) returns the same reason pre-indented, for a caller that must thread it through open() and present it later (no panel exists yet) - the one place that owns the "  " convention, so blast/panel.lua and impact/review.lua no longer re-apply it by hand.  _(2026-08-28T18:40:00Z)_
- **[invariant]** The gate on blast/panel.lua's Panel is PANEL STATE (self.gate_reason), not a one-shot query argument: `Panel:query` re-checks it (via client.supports) at the top of every path - open, a reindex, every live keypress (+/-/d/t/s) - so a gated panel never puts the method on the wire until a capable session actually appears, at which point the gate clears there and the real query goes out. `--qf`/`--loc` and `review export` both check the gate up front and report why rather than registering a hook or opening a panel that would only fire once a later session appears.  _(2026-08-28T18:40:00Z)_
