- **[command]** make test (headless suite, ~1s), make smoke (drives the real palette in a real Neovim and asserts geometry/lines/highlights), make lint (stylua --check + luacheck; each skipped with a notice when absent), make fmt  _(2026-08-27T20:04:32Z)_
- **[convention]** Tests run against tests/fake_navgraph.lua - a fake navgraph LSP server run as nvim --headless --clean -l, speaking contract v1 over stdio against tests/fixtures/proj. No zig and no network needed. Handlers are registered per area: every tests/fake/<area>.lua returns { ["navgraph/method"] = fn(ctx, params) } and tests/fake/init.lua merges them (two areas claiming one method is a hard error). Shared scanner/transport live in tests/fakelib/.  _(2026-08-27T20:04:32Z)_
- **[pitfall]** The fake server blocks on a synchronous stdin read, so libuv never delivers SIGTERM to it: client:stop(true) does NOT kill it. Always stop it gracefully (client:stop(), which sends shutdown+exit over stdio). The test harness relies on this for per-file isolation - a leaked server parks the event loop in the next spec file.  _(2026-08-27T20:04:32Z)_
- **[pitfall]** Entering insert mode with no UI attached parks the Neovim event loop, so vim.wait never returns. ui/prompt.lua skips startinsert when #nvim_list_uis() == 0; keep that guard or every headless palette test hangs.  _(2026-08-27T20:04:32Z)_
- **[convention]** Adding a feature is one file under lua/epicenter/features/ returning a spec (name/summary/commands/keymaps/options) plus one require line in lua/epicenter/features/init.lua. lua/epicenter/registry.lua is the single index that :Epicenter completion, the keymap installer, :checkhealth and the config defaults all iterate. Feature modules must NOT require epicenter.config at file scope (config builds its defaults from the registry - cycle).  _(2026-08-27T20:04:33Z)_
- **[convention]** Public UI kit modules: epicenter.ui.{window,animate,easing,theme,icons,list,tree,prompt,preview,toast,palette}. Server access is epicenter.client (one helper per navgraph/* method; every request returns a handle with :cancel(); pass channel=<name> so a newer request drops the older response as stale).  _(2026-08-27T20:04:33Z)_
- **[invariant]** The server is launched as `navgraph lsp` (contract v1). `navgraph serve` is a different protocol (agent/MCP API) - do not use it.  _(2026-08-27T20:04:33Z)_
- **[convention]** doc/epicenter.txt carries one ==== section per feature with a stable *epicenter-<feature>* tag so parallel feature work never edits the same region. tests/cases/docs_spec.lua fails if a subcommand, keymap, highlight group or top-level config key is undocumented.  _(2026-08-27T20:04:33Z)_
